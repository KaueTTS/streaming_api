package shared_constants

// Campos comuns
const (
	ID = "id"
)

// Campos de usuário
const (
	Name     = "name"
	Password = "password"
	Email    = "email"
)

// Campos de filtros
const (
	Type       = "type"
	Query      = "query"
	SortBy     = "sort_by"
	WithGenres = "with_genres"
	Language   = "language"
	Year       = "year"
)

// Campos de paginação
const (
	Page    = "page"
	PerPage = "per_page"
)
