package shared_constants

// Campos comuns
const (
	ID        = "id"
	ProfileId = "profile_id"
)

// Campos de usuário
const (
	Name     = "name"
	Password = "password"
	Email    = "email"
)

// Campos de filtros
const (
	Type     = "type"
	Query    = "query"
	SortBy   = "sort_by"
	Genre    = "genre"
	Language = "language"
	Year     = "year"
)

// Campos de paginação
const (
	Page    = "page"
	PerPage = "per_page"
)
